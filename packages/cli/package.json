{
  "name": "@agent-search/cli",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-search": "./dist/cli/src/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "search": "tsx src/search.ts",
    "compare": "tsx src/compare.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-search/core": "workspace:*",
    "@agent-search/eval": "workspace:*",
    "@agent-search/llm": "workspace:*",
    "@agent-search/sdk": "workspace:*",
    "@agent-search/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
